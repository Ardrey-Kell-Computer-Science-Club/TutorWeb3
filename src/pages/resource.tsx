import ResourcePageHeader from "../components/ResourceHeader";

export default function ResourcePage() {
  return (
    <div className="flex flex-col items-center justify-center h-screen">
      <ResourcePageHeader />
    </div>
  );
}
